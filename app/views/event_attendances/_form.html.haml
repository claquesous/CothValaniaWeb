= form_for @event_attendance do |f|
  - if @event_attendance.errors.any?
    #error_explanation
      %h2= "#{pluralize(@event_attendance.errors.count, "error")} prohibited this event_attendance from being saved:"
      %ul
        - @event_attendance.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :event_occurrence
    = f.text_field :event_occurrence
  .field
    = f.label :character
    = f.text_field :character
  .actions
    = f.submit 'Save'
