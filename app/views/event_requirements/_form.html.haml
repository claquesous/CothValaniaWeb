= form_for @event_requirement do |f|
  - if @event_requirement.errors.any?
    #error_explanation
      %h2= "#{pluralize(@event_requirement.errors.count, "error")} prohibited this event_requirement from being saved:"
      %ul
        - @event_requirement.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :requirement
    = f.text_field :requirement
  .field
    = f.label :event
    = f.text_field :event
  .actions
    = f.submit 'Save'
