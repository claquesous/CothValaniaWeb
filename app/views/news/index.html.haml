%h1.valigned News
.pull-right
  - if admin?
    = link_to 'New News', new_news_path, class: "btn btn-success"

%table.table.table-condensed
  %tr
    %th Text
    %th Date
    %th Character
    %th
    - if admin?
      %th
      %th

  - @news.each do |news|
    %tr
      %td= news.text
      %td= news.date
      %td= link_to news.member.name, member_path(news.member)
      %td= link_to 'Show', news
      - if admin?
        %td= link_to 'Edit', edit_news_path(news), class: "btn btn-info"
        %td= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"

%br

