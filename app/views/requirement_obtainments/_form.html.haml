= form_for @requirement_obtainment do |f|
  - if @requirement_obtainment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@requirement_obtainment.errors.count, "error")} prohibited this requirement_obtainment from being saved:"
      %ul
        - @requirement_obtainment.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :event
    = f.text_field :event
  .field
    = f.label :requirement
    = f.text_field :requirement
  .actions
    = f.submit 'Save'
