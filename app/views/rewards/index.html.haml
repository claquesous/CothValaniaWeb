%h1 Listing rewards

%table
  %tr
    %th Name
    %th External value
    %th Remarks
    - if leader?
      %th
      %th

  - @rewards.each do |reward|
    %tr
      %td= link_to reward.name, reward
      %td= reward.external_value
      %td= reward.remarks
      - if leader?
        %td= link_to 'Edit', edit_reward_path(reward)
        %td= link_to 'Destroy', reward, method: :delete, data: { confirm: 'Are you sure?' }

%br

- if leader?
  = link_to 'New Reward', new_reward_path
